/**
 * 归并排序
 * 以数组为例子
 */
function mergeSort(array) {
  function merge()
  function sort(target, start, end) {
    let mid = Math.floor(left + (right - left) / 2);
    
  }
  sort(array, 0, array.length - 1);
}
